generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

// Define tus modelos aquí, por ejemplo:
// model User {
//   id    Int     @id @default(autoincrement)
//   email String  @unique
//   name  String?
// }
